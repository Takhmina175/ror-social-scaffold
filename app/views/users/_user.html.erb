<li>
  Name: <%= user.name %>
  <span class="profile-link">
    <%= link_to 'See Profile',  user_path(user), class: 'profile-link' %>
    <%= link_to 'Add friend', new_friendship_path(user), class: 'btn btn-success' %>
  </span>
</li>